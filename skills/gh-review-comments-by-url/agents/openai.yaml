interface:
  display_name: "GitHub Review by URL"
  short_description: "Handle PR review comments from a provided URL"
  default_prompt: "Use a provided GitHub PR URL, fetch review comments, and address only actionable items one by one."
